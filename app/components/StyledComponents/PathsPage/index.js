import styled from 'styled-components';

const PathPageWrapper = styled.div`


`;

const TabLinkWrapper = styled.div`

`;

const StyledTabLink = styled.li`


`;

//Later on this will be styling a minimizable window.
const FilterWrapper = styled.div`


`;

//The item layout will be same so will just be li wrapped around a drop down or button depending on current tab.
const FilterItem = styled.li`

`;



export{

    PathPageWrapper,
    FilterWrapper,
    FilterItem,
    TabLinkWrapper,
    StyledTabLink,
};