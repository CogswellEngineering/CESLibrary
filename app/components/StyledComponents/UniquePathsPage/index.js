import styled from 'styled-components';

//Actually might have to be inline style
//so specific color given, but then wouldn't be doc ids, would be element in array field.

const SelectPrompt = styled.p`


`;

export{

    SelectPrompt,
}